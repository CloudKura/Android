<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="bottom"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="3dp"
        >

        <Button
            android:id="@+id/fragment_item_new_prev_button"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginEnd="5dp"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:background="@drawable/ic_button_action"
            android:drawableStart="@drawable/ic_action_previous"
            android:text="@string/regist_prev_button"
            android:textColor="@android:color/white"
            android:textSize="8pt"
            />

        <Button
            android:id="@+id/fragment_item_new_next_button"
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:layout_marginEnd="10dp"
            android:layout_marginStart="5dp"
            android:layout_weight="1"
            android:background="@drawable/ic_button_regist"
            android:drawableEnd="@drawable/ic_action_next"
            android:text="@string/regist_next_button"
            android:textColor="@android:color/white"
            android:textSize="8pt"
            />
    </LinearLayout>

</LinearLayout>